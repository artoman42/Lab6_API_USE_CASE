<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="../public/style.css">
  <title>Stock Quotes</title>
</head>
<body>
  <div class="table-container">
    <h1>Stocks table</h1>
    <table style="font-family: 'Times New Roman', serif;font-size:24px;border: 1px solid black;">
        <tr>
          <th>Symbol</th>
          <th>Short Name</th>
          <th>Sector</th>
          <th>Industry</th>
        </tr>
        <% for (let i = 0; i < quotes.length; i++) { %>
          <tr>
            <td><%= quotes[i].symbol %></td>
            <td><%= quotes[i].shortname %></td>
            <td><%= quotes[i].sector %></td>
            <td><%= quotes[i].industry %></td>
          </tr>
        <% } %>
    </table>
  </div>
</body>
</html>
